# ─── Sentinel AI Environment Configuration ───
# Copy this file to .env.local and fill in values.

# ─── Database (PostgreSQL) ───
# Railway auto-injects DATABASE_URL when a Postgres service is attached.
# For local development, point to a local or Docker Postgres instance.
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sentinel_ai

# ─── PQC Key Management ───
# 32-byte hex-encoded seed for ML-DSA-65 key generation.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SENTINEL_PQC_SEED=your_64_char_hex_string_here

# ─── AI Explainer ───
# Optional: Enables GPT-4 powered explanations. Falls back to template without this.
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini

# ─── RPC Provider ───
# Ethereum RPC URL for live chain context. Falls back to heuristic without this.
# RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# ─── Redis (Optional) ───
# If REDIS_URL is set, caching will use Redis. Otherwise falls back to PostgreSQL/in-memory.
# REDIS_URL=redis://localhost:6379

# ─── CORS ───
# Allowed origin for API requests. Defaults to * (all origins).
# CORS_ORIGIN=https://yourdomain.com

# ─── JWT Secret ───
# Secret for signing JWT tokens. Auto-generated if not set.
# JWT_SECRET=your_secret_here

# ─── Node Environment ───
NODE_ENV=development

# ─── Railway Deployment ───
# PORT is auto-injected by Railway, no need to set manually.
# RAILWAY_PUBLIC_DOMAIN=your-app.up.railway.app
